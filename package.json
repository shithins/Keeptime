{
    "name": "attendance-extension",
    "version": "1.0.0",
    "description": "Attendance Tracker",
    "scripts": {
        "build": "node build.js"
    },
    "dependencies": {
        "firebase": "^10.0.0"
    },
    "devDependencies": {
        "esbuild": "^0.19.0"
    }
}